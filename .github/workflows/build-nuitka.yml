name: Build Captain CMD Tools

on:
  workflow_dispatch:  # 仅手动触发

jobs:
  build:
    name: Build on ${{ matrix.platform.name }}
    runs-on: ${{ matrix.platform.os }}
    strategy:
      matrix:
        platform:
          - name: Windows x64
            os: windows-latest
            arch: x64
            artifact_name: captain-cmd-windows-x64
            package_ext: zip
          - name: Linux x64
            os: ubuntu-latest
            arch: x64
            artifact_name: captain-cmd-linux-x64
            package_ext: tar.gz
          - name: macOS x64
            os: macos-13
            arch: x64
            artifact_name: captain-cmd-macos-x64
            package_ext: tar.gz
          - name: macOS ARM64
            os: macos-latest
            arch: arm64
            artifact_name: captain-cmd-macos-arm64
            package_ext: tar.gz
        python-version: ['3.13']
      fail-fast: false

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
        cache: 'pip'

    - name: Setup MSVC build environment
      if: runner.os == 'Windows'
      uses: ilammy/msvc-dev-cmd@v1
      with:
        arch: amd64

    # - name: Set up MinGW (Windows)
    #   if: runner.os == 'Windows'
    #   uses: egor-tensin/setup-mingw@v2
    #   with:
    #     version: 12.2.0

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install nuitka zstandard ordered-set

    # Windows
    - name: Setup ccache (Windows)
      if: runner.os == 'Windows'
      uses: hendrikmuhs/ccache-action@v1.2
      with:
        key: ${{ runner.os }}-${{ matrix.platform.arch }}-ccache

    # Linux
    - name: Install build tools (Linux)
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y ccache patchelf

    # macOS
    - name: Setup ccache (macOS)
      if: runner.os == 'macOS'
      uses: hendrikmuhs/ccache-action@v1.2
      with:
        key: ${{ runner.os }}-${{ matrix.platform.arch }}-ccache

    - name: Build with Nuitka
      run: python build_with_nuitka.py

    - name: List build output (Windows)
      if: runner.os == 'Windows'
      run: |
        Get-ChildItem -Path .build -Recurse
      shell: pwsh

    - name: List build output (Unix)
      if: runner.os != 'Windows'
      run: |
        ls -la .build/
      shell: bash
    - name: Package artifacts (Windows)
      if: runner.os == 'Windows'
      run: |
        cd .build
        # Nuitka standalone 输出目录为 main.dist
        if (Test-Path "main.dist") {
          Rename-Item -Path "main.dist" -NewName "${{ matrix.platform.artifact_name }}"
          Compress-Archive -Path "${{ matrix.platform.artifact_name }}" -DestinationPath "${{ matrix.platform.artifact_name }}.zip"
        }
      shell: pwsh

    - name: Package artifacts (Linux)
      if: runner.os == 'Linux'
      run: |
        cd .build
        if [ -d "main.dist" ]; then
          mv main.dist ${{ matrix.platform.artifact_name }}
          tar -czf ${{ matrix.platform.artifact_name }}.tar.gz ${{ matrix.platform.artifact_name }}
        fi
      shell: bash

    - name: Package artifacts (macOS)
      if: runner.os == 'macOS'
      run: |
        cd .build
        if [ -d "main.app" ]; then
          mv main.app ${{ matrix.platform.artifact_name }}
          tar -czf ${{ matrix.platform.artifact_name }}.tar.gz ${{ matrix.platform.artifact_name }}
        elif [ -d "main.dist" ]; then
          mv main.dist ${{ matrix.platform.artifact_name }}
          tar -czf ${{ matrix.platform.artifact_name }}.tar.gz ${{ matrix.platform.artifact_name }}
        fi
      shell: bash

    # 上传构建产物
    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: ${{ matrix.platform.artifact_name }}
        path: |
          .build/${{ matrix.platform.artifact_name }}.${{ matrix.platform.package_ext }}
        retention-days: 30
        if-no-files-found: warn

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    
    steps:
    - name: Download all artifacts
      uses: actions/download-artifact@v4
      with:
        path: artifacts/

    - name: Display structure of downloaded files
      run: ls -R artifacts/

    - name: Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        files: |
          artifacts/captain-cmd-windows-x64/*.zip
          artifacts/captain-cmd-linux-x64/*.tar.gz
          artifacts/captain-cmd-macos-x64/*.tar.gz
          artifacts/captain-cmd-macos-arm64/*.tar.gz
        draft: false
        prerelease: false
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

